apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webapp-springboot.fullname" . }}-test-connection"
  labels:
    {{- include "webapp-springboot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webapp-springboot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
